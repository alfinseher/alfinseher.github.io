import React, { useState, useEffect } from 'react';

const RomanticSurprise = () => {
  const [currentPart, setCurrentPart] = useState(0);
  const [showHearts, setShowHearts] = useState(false);
  
  const messageParts = [
    "Semoga aja nanti LDR kita jadi lancar",
    "dan masi saling menjaga hati sampai kedepan nya",
    "jika kamu di pondok juga semoga harapan abang",
    "kamu bisa jaga hati juga",
    "dan sebaliknya abang jika kerja atau kuliah di luar",
    "abang bakal jaga hati buat kamu",
    "makasi ya udah mau nerima hubungan kita ini",
    "semoga kedepannya bisa menjadi lebih baik",
    "makasi sayang 💖"
  ];

  useEffect(() => {
    const timer = setTimeout(() => {
      if (currentPart < messageParts.length - 1) {
        setCurrentPart(prev => prev + 1);
      } else {
        setShowHearts(true);
      }
    }, 2000);
    
    return () => clearTimeout(timer);
  }, [currentPart]);

  return (
    <div className="min-h-screen bg-gradient-to-br from-pink-200 via-red-100 to-purple-200 flex flex-col items-center justify-center p-6 font-serif">
      {/* Romantic background elements */}
      <div className="absolute inset-0 overflow-hidden">
        <div className="absolute top-20 left-10 w-16 h-16 bg-pink-300 rounded-full opacity-20 animate-pulse"></div>
        <div className="absolute top-40 right-20 w-12 h-12 bg-red-300 rounded-full opacity-30 animate-bounce"></div>
        <div className="absolute bottom-32 left-24 w-14 h-14 bg-purple-300 rounded-full opacity-25 animate-pulse"></div>
        <div className="absolute bottom-20 right-16 w-10 h-10 bg-pink-400 rounded-full opacity-30 animate-bounce"></div>
      </div>

      <div className="bg-white/80 backdrop-blur-sm rounded-3xl shadow-2xl p-10 max-w-2xl w-full text-center relative overflow-hidden border-2 border-pink-200">
        {/* Floating hearts animation */}
        {showHearts && (
          <>
            <div className="absolute top-6 left-14 text-3xl animate-bounce text-pink-500">💖</div>
            <div className="absolute top-16 right-12 text-4xl animate-pulse text-red-500">❤️</div>
            <div className="absolute bottom-8 left-20 text-3xl animate-bounce text-purple-500">💕</div>
            <div className="absolute bottom-16 right-16 text-4xl animate-pulse text-pink-600">💘</div>
            <div className="absolute top-1/4 left-1/4 text-2xl animate-bounce text-red-400">💝</div>
            <div className="absolute bottom-1/3 right-1/4 text-2xl animate-pulse text-purple-400">💞</div>
          </>
        )}

        <div className="mb-10">
          <h1 className="text-5xl font-bold text-pink-700 mb-4 font-cursive">Untuk Adeku Tercinta 💕</h1>
          <div className="w-24 h-1 bg-gradient-to-r from-pink-500 to-purple-500 mx-auto rounded-full"></div>
        </div>

        <div className="space-y-6 mb-10">
          {messageParts.map((part, index) => (
            <div
              key={index}
              className={`text-xl text-gray-800 transition-all duration-1000 ease-in-out font-medium ${
                index <= currentPart
                  ? 'opacity-100 transform translate-y-0'
                  : 'opacity-0 transform translate-y-6'
              }`}
            >
              {part}
            </div>
          ))}
        </div>

        {showHearts && (
          <div className="animate-pulse">
            <p className="text-2xl font-semibold text-purple-700 mb-6 font-cursive">
              #DariAbangUntukAde 💖
            </p>
            <div className="flex justify-center space-x-4">
              <span className="text-4xl text-red-600 animate-bounce">❤️</span>
              <span className="text-4xl text-pink-600 animate-pulse">💖</span>
              <span className="text-4xl text-purple-600 animate-bounce">💕</span>
              <span className="text-4xl text-red-500 animate-pulse">💘</span>
            </div>
          </div>
        )}

        {!showHearts && (
          <div className="text-lg text-gray-600 mt-6 italic">
            <p>Baca pelan-pelan ya sayangku... ❤️✨</p>
          </div>
        )}
      </div>

      <style jsx>{`
        @keyframes float {
          0%, 100% { transform: translateY(0px) rotate(0deg); }
          33% { transform: translateY(-15px) rotate(5deg); }
          66% { transform: translateY(-8px) rotate(-5deg); }
        }
        .animate-bounce {
          animation: float 4s ease-in-out infinite;
        }
        .font-cursive {
          font-family: 'Dancing Script', cursive, serif;
        }
        .font-serif {
          font-family: 'Georgia', serif;
        }
      `}</style>
    </div>
  );
};

export default RomanticSurprise;